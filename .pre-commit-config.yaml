exclude: '.venv'
repos:
- repo: https://github.com/pre-commit/pre-commit-hooks
  rev: v6.0.0
  hooks:
    - id: check-yaml
    - id: check-toml
    - id: end-of-file-fixer
    - id: trailing-whitespace

- repo: local
  hooks:
  - id: ruff
    name: ruff-format
    stages: [pre-commit, pre-push]
    language: system
    entry: uv run --group benchmark ruff format .
    types: [python]
    pass_filenames: false

  - id: ruff
    name: ruff-check
    stages: [pre-commit, pre-push]
    language: system
    entry: uv run --group benchmark ruff check .
    types: [python]
    pass_filenames: false

  - id: ty
    name: ty
    stages: [pre-commit, pre-push]
    language: system
    entry: uv run --group benchmark ty check
    types: [python]
    pass_filenames: false

  - id: pytest
    name: pytest
    stages: [pre-commit, pre-push]
    language: system
    entry: uv run --group benchmark pytest
    types: [python]
    pass_filenames: false
